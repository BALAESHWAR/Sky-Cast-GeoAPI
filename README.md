# Weather-Web-Application
The application automatically detects the user’s current location using the browser’s Geolocation API and fetches accurate weather data from a public REST API. Users can also search for cities using an intelligent autocomplete feature that suggests exact and related place names while typing.
